name: Lint

on:
  pull_request:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
        with:
          enable-cache: true
          version: "0.11.28"

      - name: Set up Python
        run: uv python install

      - name: Install dev dependencies
        run: uv sync --dev --locked --all-extras --all-groups

      - name: Run ruff
        run: uv run ruff check .

      - name: Run uv format
        run: uv format --check

      - name: Run pytest
        run: uv run pytest
